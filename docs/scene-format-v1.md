# Scene format v1 (retired)

Scene format **v1** is retired. Drawable entities (`Sprite`, `Mesh`) now require a `Material` component; see **[Scene format v2](scene-format-v2.md)** for the current schema, built-in components (`Mesh`, `Material`, `RenderLayer`), and migration notes.
