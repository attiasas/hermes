package dev.hermes.core.audio;

import java.util.Optional;

/** Parsed audio profile data; loader fills this in a later task. */
final class AudioProfile {

    Optional<String> resolveClip(String clipId) {
        return Optional.empty();
    }
}
