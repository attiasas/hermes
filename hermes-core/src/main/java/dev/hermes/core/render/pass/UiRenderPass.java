package dev.hermes.core.render.pass;

import dev.hermes.api.ecs.EntityStore;
import dev.hermes.core.viewport.BoundCamera;

/**
 * Renders scene UI widget trees (see {@code UiService}); implemented in Task 6.
 */
public final class UiRenderPass {

    public void render(EntityStore entities, BoundCamera bound) {
        // Task 6 implements
    }
}
