package dev.hermes.api.ecs;

/** Field type for component inspector metadata (distinct from {@code dev.hermes.debug.FieldKind}). */
public enum FieldKind {
  FLOAT,
  INT,
  BOOLEAN,
  STRING,
  ENUM
}
